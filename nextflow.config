
profiles {
    docker {
        docker.enabled = true                           // Enable Docker
        docker.userEmulation = true
        process.container = 'custom-bwa-samtools'       // Docker image to use for the process
    }
}

workDir = 'work3'  // Directory for intermediate files
